*** Begin Patch
*** Update File: app/Controllers/StatsController.php
@@
-        // UPDATE
-        $sm->update($id, $data);
+        // Nazwa własna (opcjonalnie) + fallback z parametrów
+        $name = trim((string)$this->request->getPost('name'));
+        if ($name !== '') {
+            $data['name'] = $name;
+        } else {
+            // połącz aktualny rekord z nowymi danymi, by mieć scheme/params
+            $rowLike = array_merge($row ?? [], $data);
+            $data['name'] = $this->_schemaAutoNameFromParams(
+                (string)($rowLike['scheme'] ?? ''),
+                $rowLike['params_json'] ?? []
+            );
+        }
+
+        // UPDATE
+        $sm->update($id, $data);
*** End Patch
